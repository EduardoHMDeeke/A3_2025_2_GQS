# ConfiguraÃ§Ãµes do Banco de Dados ToolTracker
# Copie este arquivo para .env e ajuste os valores conforme necessÃ¡rio
# IMPORTANTE: O arquivo .env estÃ¡ no .gitignore e nÃ£o serÃ¡ commitado

# Host do banco de dados (padrÃ£o: localhost)
DB_HOST=localhost

# Porta do banco de dados (padrÃ£o: 3306)
DB_PORT=3306

# Nome do banco de dados (padrÃ£o: dbtooltracker)
DB_NAME=dbtooltracker

# UsuÃ¡rio do banco de dados (padrÃ£o: tooltracker)
DB_USER=tooltracker

# Senha do banco de dados (padrÃ£o: tooltracker123)
DB_PASSWORD=tooltracker123
